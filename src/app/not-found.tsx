import React from "react";
import NotFoundView from "@/components/NotFoundView";

export const metadata = {
  title: "404 — Page Not Available | LensImpact Film Club",
  description: "This page is not available on LensImpact Film Club.",
};

export default function NotFound() {
  return <NotFoundView />;
}
